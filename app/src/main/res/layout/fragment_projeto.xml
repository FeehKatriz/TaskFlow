<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.projetos.ProjetoFragment">

    <!-- FAB para criar equipe -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabCriarProjeto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="32dp"
        android:contentDescription="Criar Nova Equipe"
        android:src="@drawable/add"
        app:backgroundTint="@color/Principal"
        app:layout_constraintBottom_toBottomOf="@+id/rvProjetosEquipe"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.909"
        app:layout_constraintStart_toStartOf="parent"
        app:tint="@color/white" />

    <!-- Nome do projeto -->
    <TextView
        android:id="@+id/textView15"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="32dp"
        android:text="Project Name"
        android:textAlignment="center"
        android:textColor="#C4020202"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Layout para código do projeto com botões -->
    <LinearLayout
        android:id="@+id/layoutCodigoEquipe"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="32dp"
        android:orientation="horizontal"
        android:gravity="center"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/textView15">

        <ImageButton
            android:id="@+id/btnCopiarCodigo"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="Copiar código"
            android:src="@drawable/copiar"
            android:scaleType="centerInside"
            android:padding="6dp"
            android:layout_marginEnd="4dp" />

        <TextView
            android:id="@+id/tvCodigoEquipe"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Código: ABCD123456"
            android:textColor="#757575"
            android:textSize="16sp"
            android:padding="8dp"
            android:layout_marginEnd="8dp" />

        <ImageButton
            android:id="@+id/btnAtualizarCodigo"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="Gerar novo código"
            android:src="@drawable/girar"
            android:scaleType="centerInside"
            android:padding="6dp" />

    </LinearLayout>

    <!-- Toggle Group para alternar entre Equipes e Membros -->
    <com.google.android.material.button.MaterialButtonToggleGroup
        android:id="@+id/toggleGroup"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:layout_marginStart="30dp"
        android:layout_marginEnd="30dp"
        android:layout_marginTop="24dp"
        app:checkedButton="@id/btnProjetos"
        app:singleSelection="true"
        app:selectionRequired="true"
        app:layout_constraintTop_toBottomOf="@+id/layoutCodigoEquipe"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnProjetos"
            style="@style/ToggleButtonStyle"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="EQUIPES"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnMembros"
            style="@style/ToggleButtonStyle"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="MEMBROS"/>
    </com.google.android.material.button.MaterialButtonToggleGroup>

    <!-- RecyclerView para mostrar equipes ou membros -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvProjetosEquipe"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/toggleGroup"
        tools:itemCount="2"
        tools:listitem="@layout/item_equipes" />

</androidx.constraintlayout.widget.ConstraintLayout>